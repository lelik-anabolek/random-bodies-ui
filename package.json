{
  "name": "random-bodies-ui",
  "private": true,
  "version": "1.0.0",
  "author": "Aleksey Batuhtin, full.naked.eye@gmail.com",
  "repository": {
    "url": "https://github.com/lelik-anabolek/random-bodies-ui"
  },
  "scripts": {
    "build:wasm": "cd wasm && wasm-pack build --target web --out-dir ../ui/pkg",
    "dev": "npm run build:wasm && cd ui && npm run dev",
    "build": "npm run build:wasm && cd frontend && npm run build",
    "format:wasm": "cd wasm && cargo clippy --fix",
    "format:ui": "cd ui && npm run lint",
    "format": "npm run format:wasm && npm run format:ui"
  }
}
